using UnityEngine;
using System.Collections;

public class FrontHear : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}




	// Update is called once per frame
	void Update () {
	
		this.renderer.material.color = new Color (1,1,1,0.2f);

	}
}
